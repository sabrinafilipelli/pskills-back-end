const request = require('supertest');
const server = require('./index');
const db = require('./skillsModel');

describe('Skills', () => {
    describe('Model', () => {

    });

    describe('Router', () => {

    });
});